---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/store/viewMode.ts
  - src/components/layout/ViewModeProvider.tsx
  - src/components/tri-mode/ViewModeSwitcher.tsx
  - src/components/tri-mode/ModeAware.tsx
  - src/components/layout/ThemeProvider.tsx
  - src/components/layout/ThemeToggle.tsx
  - src/components/layout/Header.tsx
  - src/app/[locale]/layout.tsx
  - messages/en.json
  - messages/de.json
  - messages/ru.json
autonomous: true
requirements: [FOUND-04, FOUND-08]
must_haves:
  truths:
    - "Dark/light mode toggle works; system preference respected on first load; preference persists across reloads"
    - "Selecting HR, Business, or Tech mode persists in localStorage and survives a full page reload on any route"
    - "Default ViewMode on first visit is 'business'"
    - "No hydration mismatch flash when theme or mode differs from server default"
  artifacts:
    - path: "src/store/viewMode.ts"
      provides: "Zustand store with persist middleware, ViewMode type hr|business|tech, default business"
      contains: "persist"
    - path: "src/components/tri-mode/ViewModeSwitcher.tsx"
      provides: "Three-button mode toggle wired to the store"
      contains: "useViewMode"
    - path: "src/components/layout/ThemeToggle.tsx"
      provides: "Dark/light toggle via next-themes"
      contains: "useTheme"
    - path: "src/components/layout/Header.tsx"
      provides: "Header rendering ViewModeSwitcher + ThemeToggle in layout"
      contains: "ViewModeSwitcher"
  key_links:
    - from: "src/components/tri-mode/ViewModeSwitcher.tsx"
      to: "src/store/viewMode.ts"
      via: "useViewMode() hook reads/sets mode"
      pattern: "useViewMode"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/ThemeProvider.tsx"
      via: "ThemeProvider wraps children"
      pattern: "ThemeProvider"
    - from: "src/store/viewMode.ts"
      to: "localStorage"
      via: "persist middleware name 'portfolio-view-mode'"
      pattern: "portfolio-view-mode"
---

<objective>
Add the two persistent client-state slices that every later phase relies on: the dark/light theme toggle (next-themes) and the HR/Business/Tech ViewMode store (Zustand + localStorage), both rendered in the global Header so they appear on every route from the start.

This vertical slice makes a real new thing possible after Plan 01: a visitor can now toggle the color theme and switch audience mode, and both choices survive a full page reload. The ViewMode switcher UI must exist in Phase 1 even though tri-mode CONTENT is Phase 3 (CONTEXT specifics) — the toggle is part of the foundational shell.

Purpose: Establish the only two pieces of cross-page client state, with hydration-safe patterns, before any feature consumes them.
Output: A Header (in the locale layout) with a working theme toggle and a working HR/Business/Tech switcher whose state persists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan newly creates (downstream drift checks must treat these as new, not missing):
- `ViewMode` type (`'hr' | 'business' | 'tech'`) in `src/store/viewMode.ts`
- `useViewModeStore` Zustand store (persist, key `portfolio-view-mode`, default `'business'`)
- `useViewMode()` hook (re-exported from the store module) — returns `{ mode, setMode }`
- `ViewModeProvider` (client) in `src/components/layout/ViewModeProvider.tsx`
- `ViewModeSwitcher` (client) in `src/components/tri-mode/ViewModeSwitcher.tsx`
- `ModeAware` (client) in `src/components/tri-mode/ModeAware.tsx`
- `ThemeProvider` (client wrapper over next-themes) in `src/components/layout/ThemeProvider.tsx`
- `ThemeToggle` (client) in `src/components/layout/ThemeToggle.tsx`
- `Header` (client) in `src/components/layout/Header.tsx`
- messages namespaces added: `ViewMode` (label, hr, business, tech), `Theme` (toggle, light, dark, system)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ViewMode Zustand store + useViewMode hook (default business, localStorage persist)</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Component Architecture: Tri-Mode Switcher, State model lines 368-396, Hydration safety note lines 516-526)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08, D-09, D-10, D-11, D-12)
  </read_first>
  <behavior>
    - The store initial `mode` is `'business'` (D-09 — broadest appeal; note ARCHITECTURE.md shows 'hr' as an example default but CONTEXT D-09 overrides it to 'business').
    - `setMode('tech')` updates `mode` to `'tech'`.
    - The persist middleware uses localStorage key `'portfolio-view-mode'`; after setMode, the persisted value reflects the new mode.
    - ViewMode type is exactly the union `'hr' | 'business' | 'tech'` (D-12, lowercase).
    - A `useViewMode()` hook returns `{ mode, setMode }` and is the public API (D-11) — components must not import the raw store directly.
  </behavior>
  <action>
    Install Zustand: `npm install zustand@^5`.
    Create `src/store/viewMode.ts` exporting `type ViewMode = 'hr' | 'business' | 'tech'` and a `useViewModeStore` created with `create<ViewModeStore>()(persist((set) => ({ mode: 'business', setMode: (mode) => set({ mode }) }), { name: 'portfolio-view-mode' }))`. The default mode MUST be `'business'` per D-09 (not 'hr'). Per D-10 there is no auto-reset — persistence is indefinite.
    Export a `useViewMode()` hook (D-11) from the same module that returns `{ mode, setMode }` selected from the store, so consuming components use `useViewMode()` rather than touching the store directly.
    Add a colocated test `src/store/viewMode.test.ts` (use the test runner create-next-app/shadcn provided, or add `vitest` + `npm install -D vitest` if none exists) asserting the behaviors above: initial mode is 'business', setMode changes it, and the localStorage key is 'portfolio-view-mode'.
  </action>
  <verify>
    <automated>grep -q "mode: 'business'" src/store/viewMode.ts && grep -q "portfolio-view-mode" src/store/viewMode.ts && grep -q "export function useViewMode\|export const useViewMode" src/store/viewMode.ts && npx vitest run src/store/viewMode.test.ts 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>Store defaults to 'business', persists under 'portfolio-view-mode', exposes useViewMode() returning {mode,setMode}; ViewMode union is the three lowercase values; the behavior test passes (or tsc passes if no test runner is configured).</done>
</task>

<task type="auto">
  <name>Task 2: ThemeProvider + ThemeToggle (next-themes, system default, no FOUC)</name>
  <read_first>
    - .planning/research/STACK.md (Integration Gotcha 1: next-intl + next-themes, lines 320-342)
    - .planning/research/PITFALLS.md (Pitfall 9: hydration mismatch / FOUC, Pitfall 14: GA4)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14)
    - src/app/[locale]/layout.tsx (created in Plan 01 — you will edit it)
  </read_first>
  <action>
    Create `src/components/layout/ThemeProvider.tsx` as a `'use client'` thin wrapper over `next-themes` `ThemeProvider` configured `attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange` (D-14, Gotcha 1). The `<html>` already has `suppressHydrationWarning` from Plan 01 — do not remove it.
    Create `src/components/layout/ThemeToggle.tsx` as a `'use client'` component using `useTheme()` from next-themes and a shadcn `Button` with lucide `Sun`/`Moon` icons (lucide-react ships with shadcn). Guard against hydration mismatch with the mounted-state pattern (Pitfall 9): render a neutral placeholder until `useEffect(() => setMounted(true), [])` flips. The toggle cycles light ↔ dark (system is the initial resolved default).
    Edit `src/app/[locale]/layout.tsx` to wrap children with `ThemeProvider` INSIDE `NextIntlClientProvider` (the order from Gotcha 1: NextIntlClientProvider → ThemeProvider → children). Also add the `GoogleAnalytics` component from `@next/third-parties/google` with `gaId={process.env.NEXT_PUBLIC_GA_ID!}` rendered in `<body>` (CONTEXT code_context — added in Phase 1, no events yet; uses default afterInteractive so it does not block hydration per Pitfall 14). Guard so GA is omitted when `NEXT_PUBLIC_GA_ID` is unset (avoids dev noise).
  </action>
  <verify>
    <automated>grep -q "next-themes" src/components/layout/ThemeProvider.tsx && grep -q 'defaultTheme="system"' src/components/layout/ThemeProvider.tsx && grep -q "useTheme" src/components/layout/ThemeToggle.tsx && grep -q "mounted" src/components/layout/ThemeToggle.tsx && grep -q "ThemeProvider" "src/app/[locale]/layout.tsx" && grep -q "GoogleAnalytics" "src/app/[locale]/layout.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>ThemeProvider wraps children inside NextIntlClientProvider with system default + class attribute; ThemeToggle uses next-themes with the mounted-guard FOUC mitigation; GoogleAnalytics is conditionally rendered in layout body; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: ViewModeSwitcher + ModeAware + Header, wired into layout</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Switcher UI component lines 415-441, Provider placement lines 398-411, ModeAware Pattern A lines 447-478)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11)
    - src/store/viewMode.ts (created in Task 1)
    - src/app/[locale]/layout.tsx (you will edit)
    - messages/en.json, messages/de.json, messages/ru.json (you will extend)
  </read_first>
  <action>
    Create `src/components/layout/ViewModeProvider.tsx` as a `'use client'` thin component that calls `useViewModeStore()` once to trigger store hydration and renders `{children}` (ARCHITECTURE Provider placement). It exists only for SSR-safe hydration ordering.
    Create `src/components/tri-mode/ViewModeSwitcher.tsx` (`'use client'`) rendering a `role="group"` with three buttons for `['hr','business','tech']`, using `useViewMode()` to read/set mode, `aria-pressed={mode===m}` and `data-active`, with labels from `useTranslations('ViewMode')` (keys: label, hr, business, tech). Use the mounted-guard pattern so the active button matches localStorage after hydration (no flash). Style the active button with a shadcn variant.
    Create `src/components/tri-mode/ModeAware.tsx` (`'use client'`) implementing Pattern A: `{ modes: ViewMode[], children }` rendering children only when current mode ∈ modes. This is the contract Phase 3 will consume; ship it now so the interface exists.
    Create `src/components/layout/Header.tsx` (`'use client'`) rendering the site name (from `useTranslations('Common')`), a nav using next-intl `Link` from `@/i18n/navigation` (links to home/about/projects/contact — pages may not exist yet; links are fine), plus `<ThemeToggle />` and `<ViewModeSwitcher />`.
    Edit `src/app/[locale]/layout.tsx` to wrap children with `ViewModeProvider` (inside ThemeProvider) and render `<Header />` above `{children}`.
    Extend `messages/en.json`, `de.json`, `ru.json` with `ViewMode` (label, hr, business, tech) and `Theme` (toggle, light, dark) namespaces, with real DE and RU translations.
  </action>
  <verify>
    <automated>grep -q "useViewMode" src/components/tri-mode/ViewModeSwitcher.tsx && grep -q 'aria-pressed' src/components/tri-mode/ViewModeSwitcher.tsx && grep -q "modes" src/components/tri-mode/ModeAware.tsx && grep -q "ViewModeSwitcher" src/components/layout/Header.tsx && grep -q "ThemeToggle" src/components/layout/Header.tsx && grep -q "Header" "src/app/[locale]/layout.tsx" && grep -q "ViewModeProvider" "src/app/[locale]/layout.tsx" && node -e "['en','de','ru'].forEach(l=>{const m=require('./messages/'+l+'.json');if(!m.ViewMode||!m.ViewMode.hr)throw new Error('missing ViewMode in '+l)})" && npx tsc --noEmit</automated>
  </verify>
  <done>ViewModeSwitcher renders three accessible mode buttons wired to useViewMode with mounted-guard; ModeAware implements Pattern A; Header renders site name + nav + ThemeToggle + ViewModeSwitcher; layout renders Header inside ViewModeProvider; all three message files have ViewMode + Theme namespaces; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → app | Persisted ViewMode + theme values read on hydration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage view-mode value | mitigate | ViewModeSwitcher only ever sets one of the three literal union values; a tampered localStorage value falls through to no `data-active` and renders harmlessly (no content gated on it in Phase 1) |
| T-02-02 | Spoofing | next-themes class | accept | A tampered `class="dark"` only flips presentation; no security boundary, no data exposure |
| T-02-SC | Tampering | zustand install (Task 1) | mitigate | zustand is a mainstream pmndrs package verified in ARCHITECTURE.md Sources via Context7; not [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run build` succeeds.
- Toggling the ThemeToggle flips `<html class>` between (no class / dark); reload preserves the choice via next-themes storage.
- Clicking HR/Business/Tech updates `aria-pressed` and writes `portfolio-view-mode` in localStorage; reload preserves the selection on any route (verified end-to-end in Plan 04).
- No React hydration warning in the console on first load.
</verification>

<success_criteria>
- Theme toggle works, respects system preference on first load, persists across reloads.
- HR/Business/Tech selection persists in localStorage and survives reload on any route.
- Default mode on first visit is 'business'.
- Header with both controls is present on every locale route.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done, documenting the test runner used (vitest or other), confirmation of default mode 'business', and the localStorage keys for theme and view-mode.
</output>
