---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .planning/phases/01-foundation/STATIC-RENDER-CHECK.txt
  - .vercel/project.json
autonomous: false
requirements: [FOUND-01, FOUND-03]
must_haves:
  truths:
    - "After next build, all portfolio routes appear as prerendered (.html / prerendered) in .next/server/app — no dynamic SSR leak"
    - "The site is deployed and reachable on a Vercel URL"
    - "On the live Vercel deployment: /en/, /de/, /ru/ render; dark/light toggle works; HR/Business/Tech mode persists across reload; /[locale]/skeleton shows highlighted MDX"
  artifacts:
    - path: ".planning/phases/01-foundation/STATIC-RENDER-CHECK.txt"
      provides: "Recorded evidence that build output is static (prerender manifest / .html listing)"
      contains: "skeleton"
  key_links:
    - from: "next build output"
      to: ".next/server/app/[locale]"
      via: "prerendered route entries"
      pattern: "\\[locale\\]"
---

<objective>
Close the Walking Skeleton by proving the full stack works end-to-end: verify static rendering of every Phase 1 route, deploy to Vercel, and have the user confirm the complete skeleton capability on the live deployment.

This is the final vertical slice — it turns "code exists" into "the skeleton capability is demonstrably true in production." It is the gate that proves Next.js 16 boots, next-intl locale routing works on Vercel Edge, Tailwind v4 + shadcn render, the ViewMode store persists, MDX renders with highlighting, dark mode works, and the site is served by Vercel.

Purpose: Verify the static-rendering invariant (the most dangerous Phase 1 pitfall) and prove the skeleton against the real deployment target before any feature is built.
Output: A live Vercel URL where the full skeleton capability is verified, plus recorded static-render evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `.planning/phases/01-foundation/STATIC-RENDER-CHECK.txt` — recorded build static-render evidence
- `.vercel/project.json` — Vercel project link (created by `vercel link`/`vercel deploy`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build + assert static rendering of all Phase 1 routes</name>
  <read_first>
    - .planning/research/PITFALLS.md (Pitfall 2: dynamic rendering inflation — detection via .next/server/app .html vs .js; Pitfall 1: bundle size)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-18)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (versions, conventions)
  </read_first>
  <action>
    Run `npm run build`. The build MUST succeed with zero errors.
    Inspect the build output and `.next/` to confirm static rendering (D-18, Pitfall 2). In Next.js 16 the reliable signals are: (a) the build summary marks `/[locale]`, `/[locale]/skeleton` as prerendered/SSG (○ Static or ● SSG), NOT ƒ Dynamic; (b) `.next/prerender-manifest.json` (or `.next/server/app/[locale]/*.html`) contains entries for the locale home and skeleton routes. Confirm none of the portfolio routes are marked dynamic (ƒ). A dynamic mark means a `setRequestLocale` call is missing somewhere — fix it before proceeding (do NOT accept a dynamic route).
    Write the evidence to `.planning/phases/01-foundation/STATIC-RENDER-CHECK.txt`: capture the route table from the build output (the lines listing `/[locale]` and `/[locale]/skeleton` with their render symbols) and a directory listing proving prerendered output exists. The file MUST contain the substring `skeleton`.
    Also record the `First Load JS` for the home route from the build table; flag in the summary if any route exceeds 200 KB (Pitfall 1 red-flag threshold) — Phase 1 has no heavy client libs so this should be well under.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/STATIC-RENDER-CHECK.txt && grep -q "skeleton" .planning/phases/01-foundation/STATIC-RENDER-CHECK.txt && ! grep -E '\bƒ\b.*\[locale\]|Dynamic.*\[locale\]' .planning/phases/01-foundation/STATIC-RENDER-CHECK.txt</automated>
  </verify>
  <done>npm run build succeeds; /[locale] and /[locale]/skeleton are prerendered (not dynamic); STATIC-RENDER-CHECK.txt records the route table + prerender evidence and contains 'skeleton'; no portfolio route is marked dynamic.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel</name>
  <read_first>
    - .planning/research/STACK.md (Deployment section — zero-config, no vercel.json, middleware on Edge)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-21: no output: 'export'; D-22: no sharp)
  </read_first>
  <action>
    Deploy via the Vercel CLI (this is automatable — do NOT make the user click through a dashboard). Ensure the CLI is available: `npx vercel --version` (or `npm i -g vercel`). Run `npx vercel link` if not yet linked, then `npx vercel --prod --yes` to deploy. Do NOT create a `vercel.json` (zero-config Next.js detection — STACK.md). Confirm `output: 'export'` is absent from next.config.mjs (D-21) before deploying — static export would break next-intl middleware.
    If the CLI returns an authentication error, STOP and emit an authentication gate (the user runs `npx vercel login`, then you retry) — do not treat auth failure as a deploy failure.
    Capture the resulting production URL and record it for Task 3. Confirm `.vercel/project.json` was created (project linked).
  </action>
  <verify>
    <automated>test -f .vercel/project.json && grep -q "projectId" .vercel/project.json</automated>
  </verify>
  <done>The project is linked to Vercel (.vercel/project.json with projectId exists) and a production deploy URL was produced and recorded for the verification checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full Walking Skeleton capability on the live deployment</name>
  <what-built>
    A deployed Next.js 16 site on Vercel with next-intl locale routing (/en/, /de/, /ru/), Tailwind v4 + shadcn/ui, next-themes dark/light, a Zustand HR/Business/Tech ViewMode store with localStorage persistence, and a build-time MDX-rendered skeleton page with syntax highlighting.
  </what-built>
  <how-to-verify>
    Open the production Vercel URL recorded in Task 2 and confirm each of the five Phase 1 success criteria:
    1. Locale routing: visit `<url>/en/`, `<url>/de/`, `<url>/ru/` — each renders the home page with locale-correct strings. Confirm the `/ru/` page renders cyrillic nav labels in the proper Inter font (not a system fallback). Confirm `<url>/favicon.ico` loads (not redirected to a locale 404).
    2. Static render: pages load instantly with no SSR cold-start stutter (already asserted in Task 1 build output).
    3. Dark mode: click the theme toggle in the header — the page switches dark/light. Reload — the choice persists. In a fresh/incognito window, confirm it respects your OS appearance setting on first load.
    4. ViewMode: click HR, then Business, then Tech in the header switcher — the active button updates. Reload the page on any route (e.g. on `/en/skeleton`) — the selected mode is still active. Confirm the default in a fresh incognito window is Business.
    5. MDX: visit `<url>/en/skeleton` — confirm prose renders and the code block is syntax-highlighted; toggle dark mode and confirm the code block re-themes. Visit `<url>/de/skeleton` and `<url>/ru/skeleton` — they render (English fallback), not 404.
  </how-to-verify>
  <resume-signal>Type "approved" if all five criteria pass, or describe which criterion failed (with the URL and what you saw) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → Vercel deploy | Code and env vars pushed to Vercel's build/runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | Vercel CLI auth | mitigate | Deploy uses authenticated Vercel CLI session; auth failure triggers an explicit login gate rather than silent fallback |
| T-04-02 | Information Disclosure | env vars in deploy | accept | Phase 1 has no secrets; NEXT_PUBLIC_GA_ID is intentionally public; no private keys exist yet |
| T-04-03 | Tampering | static export breaking middleware | mitigate | Verify `output: 'export'` absent before deploy (D-21) — preserves Edge middleware security/redirect behavior |
</threat_model>

<verification>
- `npm run build` is clean and all portfolio routes are prerendered (Task 1).
- A production Vercel URL is live (Task 2).
- All five Phase 1 success criteria pass on the live URL (Task 3 human verification).
</verification>

<success_criteria>
- All Phase 1 ROADMAP success criteria are TRUE on the live Vercel deployment.
- Build output proves static rendering (no dynamic SSR leak).
- The Walking Skeleton capability is demonstrably true in production.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done, recording the live Vercel production URL, the static-render evidence summary, the home route First Load JS, and the user's verification result.
</output>
