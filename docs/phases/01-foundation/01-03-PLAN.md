---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - mdx-components.tsx
  - src/lib/content.ts
  - src/content/pages/en/skeleton.mdx
  - src/app/[locale]/skeleton/page.tsx
autonomous: true
requirements: [FOUND-06, FOUND-07]
must_haves:
  truths:
    - "An MDX file renders at /en/skeleton at build time (server-rendered, not client-only)"
    - "A code block inside the MDX renders with rehype-pretty-code syntax highlighting that responds to dark/light theme"
    - "Missing locale MDX falls back to the English file rather than 404 (single-English-MDX strategy)"
  artifacts:
    - path: "mdx-components.tsx"
      provides: "Root MDX component map (required at project root)"
      contains: "useMDXComponents"
    - path: "src/lib/content.ts"
      provides: "getPageContent(slug, locale) with English fallback via gray-matter"
      contains: "gray-matter"
    - path: "src/content/pages/en/skeleton.mdx"
      provides: "Skeleton MDX page with prose + a fenced code block"
      contains: "```"
    - path: "src/app/[locale]/skeleton/page.tsx"
      provides: "Server Component rendering compiled MDX with setRequestLocale + generateStaticParams"
      contains: "compileMDX"
  key_links:
    - from: "src/app/[locale]/skeleton/page.tsx"
      to: "src/lib/content.ts"
      via: "getPageContent(slug, locale) read"
      pattern: "getPageContent"
    - from: "src/app/[locale]/skeleton/page.tsx"
      to: "next-mdx-remote/rsc"
      via: "compileMDX build-time render"
      pattern: "compileMDX"
---

<objective>
Stand up the MDX content pipeline end-to-end by rendering one skeleton MDX page at /en/skeleton with syntax-highlighted code, proving the build-time MDX → Server Component path and the single-English-MDX localization strategy.

This vertical slice makes a new thing possible after Plan 01: a visitor can navigate to a real MDX-backed page with a highlighted code block. It establishes the exact content-loading convention (gray-matter + compileMDX in a Server Component, English fallback) that Phases 2-5 will reuse for projects/blog/ADR — without proliferating per-locale files (D-04, D-07).

Purpose: Prove the MDX pipeline and lock the localization strategy before any real content is authored.
Output: A statically prerendered /[locale]/skeleton MDX page with working syntax highlighting and locale fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan newly creates (downstream drift checks must treat these as new, not missing):
- `useMDXComponents` export in root `mdx-components.tsx` (returns the global MDX component map)
- `getPageContent(slug, locale)` in `src/lib/content.ts` — returns `{ frontmatter, content, slug }` with English fallback
- `getPageSlugs(locale)` in `src/lib/content.ts`
- `src/content/pages/en/skeleton.mdx` — first MDX content file (prose + fenced code block)
- `SkeletonPage` (default export of `src/app/[locale]/skeleton/page.tsx`) + `generateStaticParams` + `dynamicParams = false`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Root MDX component map + content loader with English fallback</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (MDX Content Organization lines 240-364, MDX loading pattern, content strategy locale-per-file with fallback)
    - .planning/research/STACK.md (Content Layer MDX, next.config.mjs plugin order — already set in Plan 01)
    - .planning/research/PITFALLS.md (Pitfall 3: content architecture, Pitfall 16: dynamicParams)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04, D-07)
    - next.config.mjs (Plan 01 already configured withMDX with rehype-pretty-code)
  </read_first>
  <action>
    Create `mdx-components.tsx` at the PROJECT ROOT (Next.js requires this exact location) exporting `useMDXComponents(components)` returning a merged component map. Start minimal — pass through defaults plus any shadcn typography wrappers you want; the map grows in later phases.
    Create `src/lib/content.ts` with `getPageSlugs(locale = 'en'): string[]` and `getPageContent(slug, locale = 'en')` following ARCHITECTURE's pattern: resolve `src/content/pages/${locale}/${slug}.mdx`, and if it does not exist fall back to `src/content/pages/en/${slug}.mdx` (D-04 single-English-MDX strategy / Pitfall 3 — never 404 a missing translation). Use `gray-matter` to split frontmatter from content and return `{ frontmatter, content, slug }`. Use `fs` + `path` with `process.cwd()` as the root (Server Component / build-time only — never import this into a client component, Pitfall: client-side MDX fetching).
  </action>
  <verify>
    <automated>test -f mdx-components.tsx && grep -q "useMDXComponents" mdx-components.tsx && grep -q "gray-matter\|gray_matter\|matter(" src/lib/content.ts && grep -q "getPageContent" src/lib/content.ts && grep -q "existsSync" src/lib/content.ts && npx tsc --noEmit</automated>
  </verify>
  <done>mdx-components.tsx exists at root with useMDXComponents; content.ts provides getPageContent + getPageSlugs with existsSync-based English fallback via gray-matter; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton MDX file + Server Component route with build-time render + highlighting</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Dynamic MDX rendering in page lines 332-364, generateStaticParams locale×slug, dynamicParams = false)
    - .planning/research/STACK.md (Code Highlighting dual-theme CSS — already added to globals.css in Plan 01; rehype-pretty-code theme config in next.config)
    - .planning/research/PITFALLS.md (Pitfall 2: setRequestLocale, Pitfall 7: locale×slug params, Pitfall 16: dynamicParams)
    - src/lib/content.ts (Task 1)
    - mdx-components.tsx (Task 1)
  </read_first>
  <action>
    Create `src/content/pages/en/skeleton.mdx` with frontmatter (`title`, `summary`) and body containing at least one paragraph of prose and one fenced TypeScript code block (e.g. a `function hello(name: string)` snippet) so syntax highlighting is exercised. This is the only locale file — DE/RU will fall back to it (proves D-04).
    Create `src/app/[locale]/skeleton/page.tsx` as an async Server Component that: (1) `const { locale } = await params`; (2) calls `setRequestLocale(locale)` first (Pitfall 2); (3) exports `generateStaticParams` returning `routing.locales.map((locale) => ({ locale }))` (single slug, all locales — locale×slug pattern from Pitfall 7); (4) exports `export const dynamicParams = false` (Pitfall 16); (5) loads content via `getPageContent('skeleton', locale)`; (6) renders it with `compileMDX({ source: content, components: useMDXComponents({}), options: { parseFrontmatter: false } })` from `next-mdx-remote/rsc` (ARCHITECTURE — parseFrontmatter false because gray-matter already split it); (7) renders the compiled content inside a simple article layout. The code block must be highlighted by rehype-pretty-code at build time (zero runtime cost) and respond to the dark/light class via the dual-theme CSS added in Plan 01.
  </action>
  <verify>
    <automated>test -f "src/content/pages/en/skeleton.mdx" && grep -qF '```' "src/content/pages/en/skeleton.mdx" && grep -q "setRequestLocale(locale)" "src/app/[locale]/skeleton/page.tsx" && grep -q "compileMDX" "src/app/[locale]/skeleton/page.tsx" && grep -q "dynamicParams = false" "src/app/[locale]/skeleton/page.tsx" && grep -q "generateStaticParams" "src/app/[locale]/skeleton/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>skeleton.mdx has frontmatter + a fenced code block; skeleton/page.tsx is a Server Component calling setRequestLocale, exporting generateStaticParams over all locales + dynamicParams=false, loading via getPageContent, and rendering via compileMDX; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem MDX → build pipeline | MDX content compiled at build time into HTML |
| URL slug → page route | Untrusted [slug]/[locale] segment selects an MDX file path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | URL → file path in getPageContent | mitigate | `dynamicParams = false` + `generateStaticParams` enumerate only known slugs; a forged slug 404s at build-static boundary and never reaches fs.readFile with attacker input |
| T-03-02 | Information Disclosure | path traversal via slug | mitigate | content.ts resolves only within `src/content/pages/${locale}/` and slugs come exclusively from generateStaticParams; no user-supplied slug reaches readFileSync at runtime (static-only) |
| T-03-03 | Tampering | MDX-embedded JSX | accept | All MDX is author-controlled (repo content), not user-submitted; no untrusted MDX is compiled |
| T-03-SC | Tampering | next-mdx-remote/gray-matter (installed Plan 01) | mitigate | Both verified in STACK.md Sources via Context7/npm; not [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run build` succeeds and prerenders /en/skeleton, /de/skeleton, /ru/skeleton.
- Visiting /en/skeleton shows the prose + a syntax-highlighted code block.
- Visiting /de/skeleton and /ru/skeleton render (falling back to the English MDX) rather than 404.
- View-source on /en/skeleton shows the highlighted code markup in the HTML (build-time, not client-only).
</verification>

<success_criteria>
- MDX file renders at /[locale]/skeleton with syntax-highlighted code blocks at build time.
- DE/RU fall back to the English MDX (no 404, no per-locale file proliferation).
- The content-loading convention (gray-matter + compileMDX + English fallback) is established for later phases.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done, documenting the content directory layout chosen, the compileMDX options used, and confirmation that DE/RU fall back to English.
</output>
